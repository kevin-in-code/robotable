{
  "name": "robotable",
  "version": "1.0.0",
  "description": "Simulation of a robot moving on a table",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf coverage dist",
    "build": "tsc -p .",
    "test": "jest --coverage",
    "dev": "ts-node src/index.ts",
    "debug": "nodemon -L src/index.ts",
    "start": "cross-env NODE_ENV=production NODE_PATH=./dist node dist/src/index.js"
  },
  "author": "Kevin A. Naudé",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^16.9.2",
    "cross-env": "^7.0.3",
    "jest": "^27.2.0",
    "nodemon": "^2.0.12",
    "rimraf": "^3.0.2",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "ts-jest": "^27.0.5"
  }
}
